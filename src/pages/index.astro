<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Join WinNow - Get Rewards!</title>
  <style>
    :root {
      --primary: #0066ff;
      --secondary: #f4f7fc;
      --accent: #00cc99;
    }

    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: var(--secondary);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .card {
      background: #fff;
      border-radius: 16px;
      padding: 30px 25px;
      max-width: 400px;
      width: 90%;
      box-shadow: 0 10px 25px rgba(0,0,0,0.1);
      text-align: center;
    }

    .logo {
      font-size: 1.8rem;
      color: var(--primary);
      font-weight: bold;
    }

    h2 {
      margin-top: 10px;
      color: #333;
    }

    .ref-box {
      margin: 20px 0;
      background: var(--accent);
      color: white;
      padding: 10px 20px;
      border-radius: 8px;
      font-size: 1.2rem;
      font-weight: bold;
      display: inline-block;
    }

    input[type="tel"] {
      padding: 12px;
      width: 100%;
      margin-top: 10px;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 1rem;
    }

    button {
      margin-top: 15px;
      background: var(--primary);
      color: white;
      border: none;
      padding: 12px;
      width: 100%;
      font-size: 1rem;
      border-radius: 8px;
      cursor: pointer;
    }

    button:hover {
      background: #004ecb;
    }

    .small-text {
      margin-top: 15px;
      font-size: 0.85rem;
      color: #777;
    }

    @media (max-width: 500px) {
      .card {
        padding: 25px 15px;
      }
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="logo">WinNow</div>
    <h2>Claim Your Welcome Bonus!</h2>
    <p>Use this exclusive referral code to get started:</p>
    <div class="ref-box">REF2025</div>

    <form action="#" method="POST">
      <input type="tel" name="phone" placeholder="Enter your phone number" required />
      <button type="submit">Join Now</button>
    </form>

    <div class="small-text">
      By signing up, you agree to our <a href="#">Terms & Privacy</a>.
    </div>
  </div>
</body>
</html>
